# from main.models import Listing
#
# def extra_app_context(request):
#     new_hits = Listing.objects.filter(acknowledged=False).count()
#     return {
#         'new_hits' : new_hits
#     }
#todo probably unnecessary